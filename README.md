# Music-Generator
Generates and plays files given a user's specifications

To build, create a directory, and run "cmake /path/to/Music-Generator".
After that's done, run "make" in that directory. It should put the binary right there.
